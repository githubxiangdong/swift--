//: Playground - noun: a place where people can play

import UIKit

/*
 类型推导
 1> 如果在定义一个标识符时，有直接给该标识符赋值，那么可以将标识符后面的类型付省略
 2> 编译器会根据后面赋值的类型，推导出前面标识符的类型，这个过程就叫做类型推导
 3> 可以通过 option + 鼠标左键，查看标识符类型
 */
let a = 4
var b = 3.12

let view = UIView()